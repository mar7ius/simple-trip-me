<%= @trip.destination %>

<%= simple_form_for @trip, url: flight_choice_trip_path(@trip), method: :post do |f| %>
  <%= f.association :trip_flights, as: :check_boxes, collection: Flight.all, label_method: lambda { |flight| render("flight_infos", flight: flight)  }, value_method: :id, class: ''%>
  <%# Flight.all.each do |flight| %>
  <%# flight.departure %>
  <%#= flight.departure, flight.departure_date, flight.arrival, flight.arrival_date, flight.price, flight.flight_company, flight.duration %>
  <%# end %>
  <%= f.submit class: 'btn btn-flat', label: "Choose your flight" %>
<% end %>
